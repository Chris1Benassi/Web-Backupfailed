---
const { width = 100 } = Astro.props; // Default width is 150px
const height = width / 3; // Maintain aspect ratio (assuming 3:1 ratio for your logo)
---

<img
    src="https://storage001backupfailed.blob.core.windows.net/stor-container-web/LOGO/BFS%20Transparent%20Project.png"
    alt="Backup Failed Logo"
    width={width}
    height={height}
    class="block mx-auto"
/>
